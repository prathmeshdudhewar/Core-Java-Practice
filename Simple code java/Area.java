//find area  Area=length*breadth;
public class Area
{
	public static void main(String x[])
	{
	 int length=Integer.parseInt(x[0]);
	 int breadth=Integer.parseInt(x[1]);
	 int area=length*breadth;
	 System.out.println("The Area of Rectangle is =" +area);
	}
}